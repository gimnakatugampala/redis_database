<h1>{{user.first_name}} {{user.last_name}} [{{user.id}}]</h1>
<ul class="list-group">
    <li class="list-group-item"><strong>Email:</strong>{{user.email}}</li>
    <li class="list-group-item"><strong>Phone:</strong>{{user.phone}}</li>
</ul>

<a href="/" class="btn btn-default pull-left">Back</a>
<form action="/user/delete/{{user.id}}?_method=DELETE" method="POST">
    <input type="submit" class="btn btn-danger pul-right" value="Delete">
</form>